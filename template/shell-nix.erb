with (import <nixpkgs> {});
let
  gems = bundlerEnv {
    name = "<%= project %>-bundler-env";
    inherit <%= ruby %>;
    gemfile  = <%= gemfile_path %>;
    lockfile = <%= lockfile_path %>;
    gemset   = <%= gemset_path %>;
  };
in mkShell {
  buildInputs = [ gems gems.wrappedRuby ];
}
